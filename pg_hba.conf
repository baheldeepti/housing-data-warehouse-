# Allow socket connections inside the container without a password
local   all  all                        trust
# Require a password for every TCP connection (host network)
host    all  all  0.0.0.0/0             md5
host    all  all  ::/0                  md5
